# ===== AlphaResearch DEMO config =====
# The demo runs fully standalone — no backend, no auth, no API keys.
# The mock streaming server lives in-app at /api/mock, so there is nothing to set.
# (Left intentionally minimal; copy to .env.local only if you want to override.)
