# pnpm 11 reads build/script settings from here (not the package.json "pnpm" field).
# Approve sharp's native build (Next image optimization) and don't re-run install
# as a pre-step of `pnpm run`, which otherwise escalates the ignored-build notice.
only-built-dependencies[]=sharp
verify-deps-before-run=false
